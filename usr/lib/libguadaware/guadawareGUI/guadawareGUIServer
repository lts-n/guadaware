#!/bin/sh
set -e

command -v python3 >/dev/null 2>&1 || {
    echo "error: required command 'python3' not found" >&2
    exit 1
}

cd "$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)" && exec python3 -m http.server 8000 --bind 127.0.0.1
